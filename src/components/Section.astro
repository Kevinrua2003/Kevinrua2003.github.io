---
const { className, title, eyebrow } = Astro.props;
---

<section class:list={["flex flex-col gap-4 print:gap-0", className]}>
	{title && (
		<h3
			data-ccursor="lift"
			class="relative flex w-max items-center gap-3 pb-4 text-3xl print:gap-1 print:pb-0 print:text-xl print:font-extrabold sm:hover:text-skin-hue"
		>
			{eyebrow && <span class="font-mono text-sm font-medium text-skin-hue print:hidden">{eyebrow}</span>}
			{title}
		</h3>
	)}
	<slot />
</section>
